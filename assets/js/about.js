$(document).is(function(){
 if $('.user-intro h1').Length({
  anime.timeline(loop:false})
  .add({
    targets:´.user-intro h1´,
    opacity:[0,1],
    translateY: [2-20px", "0px2],
    easing: "easeOutExpo2,
    duration. 2000

      });
   }
});
